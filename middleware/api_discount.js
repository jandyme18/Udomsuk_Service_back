const express = require('express');
const mongoose = require('mongoose');
const router = express.Router();
const Discounts = require('../models/discount_schema')
const crypto = require("crypto");


module.exports = router;